///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////





//-------------------------------------------------------------------------------------------------------------------//


export async function getArea(parentId = null) {

	let url = "/api/local";

	if (parentId) {

		url = `${url}?parentId=${parentId}`;
	}

	console.log(url);

	return await fetch(url)
		.then(resp => resp.json());
}


///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
